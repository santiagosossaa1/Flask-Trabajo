# Sistema de facturación — Flask + SQLite + AdminLTE

## Ejecutar en desarrollo
```bash
pip install -r requirements.txt
python app.py
```

Usuarios demo:
- admin@facturas.com / admin
- usuario@facturas.com / usuario
